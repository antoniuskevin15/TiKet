import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.tiket.kelompok1',
  appName: 'TikeT',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
